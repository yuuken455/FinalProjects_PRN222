@page
@model RazorPage.Pages.Customer.Vehicle.CreateModel
@{
    ViewData["Title"] = "Thêm xe mới";
}

<style>
  /* Trang trí nhẹ để form gọn gàng, ưu tiên dùng Bootstrap có sẵn */
  .create-vehicle-container { max-width: 760px; }
  .vin-input, .plate-input { text-transform: uppercase; }
</style>

<div class="create-vehicle-container mx-auto">
    <h1 class="mb-3">Thêm xe mới</h1>

    @if (!string.IsNullOrWhiteSpace(Model.ErrorMessage))
    {
        <div class="alert alert-danger" role="alert">
            <strong>Lỗi:</strong> @Model.ErrorMessage
        </div>
    }

    <div asp-validation-summary="ModelOnly" class="text-danger"></div>

    <form method="post" class="needs-validation" novalidate>
        @* CustomerId lấy từ Session, không cho sửa *@
        <input asp-for="CreateVehicleDto.CustomerId" type="hidden" />

        <div class="card shadow-sm">
            <div class="card-body">
                <div class="mb-3">
                    <label asp-for="CreateVehicleDto.Model" class="form-label">Mẫu xe</label>
                    <input asp-for="CreateVehicleDto.Model" class="form-control" placeholder="Ví dụ: Tesla Model 3" required />
                    <span asp-validation-for="CreateVehicleDto.Model" class="text-danger"></span>
                </div>

                <div class="mb-3">
                    <label asp-for="CreateVehicleDto.Vin" class="form-label">Số VIN</label>
                    <input asp-for="CreateVehicleDto.Vin" class="form-control vin-input" placeholder="Ví dụ: 5YJ3E1EA7JF000000" />
                    <span asp-validation-for="CreateVehicleDto.Vin" class="text-danger"></span>
                </div>

                <div class="mb-3">
                    <label asp-for="CreateVehicleDto.LicensePlate" class="form-label">Biển số</label>
                    <input asp-for="CreateVehicleDto.LicensePlate" class="form-control plate-input" placeholder="Ví dụ: 30F-123.45" required />
                    <span asp-validation-for="CreateVehicleDto.LicensePlate" class="text-danger"></span>
                </div>

                <div class="mb-3">
                    <label asp-for="CreateVehicleDto.CurrentKm" class="form-label">Số km hiện tại</label>
                    <div class="input-group">
                        <input asp-for="CreateVehicleDto.CurrentKm" class="form-control" type="number" step="0.1" min="0" placeholder="Ví dụ: 12345" />
                        <span class="input-group-text">km</span>
                    </div>
                    <span asp-validation-for="CreateVehicleDto.CurrentKm" class="text-danger"></span>
                </div>
            </div>
            <div class="card-footer d-flex gap-2 justify-content-end">
                <a asp-page="/Customer/Vehicle/Index" class="btn btn-outline-secondary">Hủy</a>
                <button type="submit" class="btn btn-primary">Lưu</button>
            </div>
        </div>
    </form>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
    <script>
        (() => {
            'use strict';
            const forms = document.querySelectorAll('.needs-validation');
            Array.from(forms).forEach(form => {
                form.addEventListener('submit', event => {
                    if (!form.checkValidity()) {
                        event.preventDefault();
                        event.stopPropagation();
                    }
                    form.classList.add('was-validated');
                }, false);
            });
        })();
    </script>
}
